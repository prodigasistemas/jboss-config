<!-- SSO Cluster PROTOCOL STACK is a replica of the default stack used by the  
    JBoss cluster service, with an optional ENCRYPT protocol added below GMS -->

<!-- ************ JGroups Protocol Stack Configuration ************** -->
<!-- generated by XmlConfigurator on Wed May 19 22:26:17 PDT 2004 -->
<!-- input file: sso-channel.xml -->

<config>
    <!-- UDP: if you have a multihomed machine, comment out the first protocol version,
         uncomment the second and set the bind_addr attribute to the appropriate NIC IP address -->
    <!-- UDP: On Windows machines, because of the media sense feature
         being broken with multicast (even after disabling media sense)
         set the loopback attribute to true -->
    <UDP mcast_recv_buf_size="800000" mcast_send_buf_size="800000" mcast_port="45567" 
         ucast_recv_buf_size="800000" mcast_addr="228.8.8.8" loopback="false" 
         ucast_send_buf_size="800000" ip_ttl="32"/> 
    
    <!--
    <UDP bind_addr="192.168.10.10" mcast_recv_buf_size="800000" mcast_send_buf_size="800000" mcast_port="45567" 
         ucast_recv_buf_size="800000" mcast_addr="228.8.8.8" loopback="false" 
         ucast_send_buf_size="800000" ip_ttl="32"/>
     -->
     
    <PING timeout="2000" down_thread="true" num_initial_members="3" up_thread="true"/>
    <MERGE2 max_interval="20000" min_interval="10000"/>
    <FD timeout="2500" max_tries="5" down_thread="true" up_thread="true" shun="true"/>
    <VERIFY_SUSPECT timeout="3000" num_msgs="3" down_thread="true" up_thread="true"/>
    <pbcast.NAKACK max_xmit_size="8192" down_thread="true" gc_lag="50" up_thread="true" 
         retransmit_timeout="600,1200,2400,4800"/>
    <UNICAST timeout="1200,2400,3600" down_thread="true" min_threshold="10" window_size="100"/>
    <pbcast.STABLE stability_delay="1000" desired_avg_gossip="20000" down_thread="true" 
         max_bytes="0" up_thread="true"/>
    <FRAG frag_size="8192" down_thread="true" up_thread="true"/>
   <!-- Uncomment the Encryption protocol to ensure SSO credential information
         is encrypted as it is passed around the cluster. 
         NOTE: JDK 1.4 is needed for this protocol to function!! -->
    <!--ENCRYPT1_4 symInit="56" asymInit="512" symAlgorithm="DES/ECB/PKCS5Padding" asymAlgorithm="RSA"/ -->
    <pbcast.GMS print_local_addr="true" join_timeout="5000" join_retry_timeout="2000" shun="true"/>
    <pbcast.STATE_TRANSFER down_thread="true" up_thread="true"/>
</config>
